const radius = 8.78;
const height = 24.333;

const cylinderVolume = (Math.PI * Math.pow(radius, 2) * height).toFixed(2);

console.log("The radius of the cylinder:", radius);
console.log("The height of the cylinder:", height);
console.log("The volume of the cylinder:", cylinderVolume);